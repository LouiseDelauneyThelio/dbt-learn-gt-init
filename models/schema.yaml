models:
  - name: customers
    description: One record per customer with aggregated order info
    semantic_model:
      enabled: true
    agg_time_dimension: first_order_date
    columns:
      - name: customer_id
        description: Primary key
        entity:
          name: customer
          type: primary
        tests:
          - unique
          - not_null
      - name: first_name
        description: "Customer's first name"
        dimension:
          type: categorical
      - name: last_name
        description: "Customer's last name"
        dimension:
          type: categorical
      - name: first_order_date
        description: Date of the customer's first order
        granularity: day
        dimension:
          type: time
      - name: most_recent_order_date
        description: Date of the customer's most recent order
        granularity: day
        dimension:
          type: time
      - name: number_of_orders
        description: Count of orders placed by this customer
    metrics:
      - name: customer_count
        label: Customer Count
        description: Count of customers
        type: simple
        agg: count
        expr: 1


  - name: time_spine_daily
    description: Daily time spine covering five years of history and 30 days ahead.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Calendar date at daily grain.
        granularity: day
